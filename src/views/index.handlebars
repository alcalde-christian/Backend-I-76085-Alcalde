{{!-- Listado no dinámico (se necesita recargar) de los productos almacenados en la BD --}}
<div class="productsContainer">
    {{#each products}}
        <div class="productCard">
            <p>
                {{this.title}}
            </p>
            <p>
                {{this.description}}
            </p>
            <p>
                $ {{this.price}}
            </p>
        </div>
    {{/each}}
</div>